# TODO — 広告の導入

まだ実装しない。公開後、アクセスが付いてから着手する。
ここに書いてあるのは「やると決めた形」と「やらないと決めた形」の記録。

---

## 方針

| | 形式 | 判断 |
|---|---|---|
| ✅ | **バナー広告**（パネル内、AdSense） | 薄く広く。地図は覆わない |
| ✅ | **インフィード広告**（地点情報の直下、PRカード） | 収益の本命。文脈に合うものだけ |
| ❌ | インタースティシャル（全面広告） | 下記の理由により採用しない |
| ❌ | アンカー広告（画面下追従） | 地図を常時覆うため |
| ❌ | ポップアップ | 論外 |

### 各区GISへ飛ぶ時の全面広告を採用しない理由

1. **AdSenseポリシー違反リスク** — Googleは「コンテンツを妨害する広告」「クリックのたびの全面広告」を禁止している。アカウント停止時は将来のGoogle収益化サービスも使えなくなる
2. **検索順位が下がる** — モバイルのインタースティシャルペナルティ（ページ単位で適用）
3. **一番の資産に通行料をかけることになる** — 各区公式GISへの地点付きディープリンクは、全国展開の競合が真似できない唯一の資産（千代田区・杉並区の座標系問題まで実地で解決済み）。ここに壁を作るのは、リピートと口コミが生まれる瞬間を潰す取引になる

---

## 着手前に必ず片づけること

- [ ] **独自ドメインへの移行**（→ 詳細は後述。広告より先にやる）
- [ ] **プライバシーポリシーのページ**
      広告Cookieを使う以上必須。AdSense審査でも要求される
- [ ] **免責事項のページ**
      現在は `#credits` に1行あるだけ。用途地域は実際の判断に使われるため、独立したページにまとめる
- [ ] **解説記事を数本**
      1ページの地図ツールは AdSense 審査で「価値の低い広告枠」として落ちることが実際にある。
      対策と検索流入を兼ねて用意する:
      - 用途地域の読み方（13種類の一覧と、何が建てられるか）
      - 建ぺい率と容積率の違い
      - 高度地区の斜線制限とは何か（既に `#result` に出している北側斜線の注意の背景）
      - 防火地域・準防火地域で何が変わるか
- [ ] **ads.txt** の設置（AdSense 承認後に指示される内容を `site/ads.txt` へ）

### ライセンス面（確認済み・問題なし）

収益化して差し支えない。42条道路の調査時に見つかったような「営利目的での利用を禁止」条項は、
今使っている3つのデータ源のいずれにも無い。

| データ | 条件 |
|---|---|
| 東京都「都市計画決定情報GISデータ」 | CC BY 4.0（商用可・出典表示） |
| 国交省「街区レベル位置参照情報」 | 無償・出典明記のみ。営利禁止条項なし |
| 国交省 国土数値情報「行政区域データ」 | 政府標準利用規約準拠（商用可） |

---

## 1. バナー広告

**置き場所**: `site/index.html` の `#panel` 内、`#resultBlock`（地点の情報）の**後**、`#credits` の**前**。
新しい `<section class="block">` として1枠だけ。

```
#panel
  ├─ #brand（タイトル）
  ├─ .block（住所で探す ／ 選んで探す）
  ├─ .block（表示する地域・凡例）
  ├─ .block #resultBlock（地点の情報）
  ├─ .block ← ★ここにバナー1枠
  └─ #credits
```

**理由**: 地図（`#mapWrap`）には一切重ねない。答えを見た後に目に入る位置で、
判断の邪魔をしない。

**注意点**:
- モバイルは `@media (max-width: 760px)` でパネルが `46vh` 固定＋独自スクロール
  （`site/style.css`）。バナーを入れると「地点の情報」が押し下がるので、
  `inspect()` 末尾の `$('#resultBlock').scrollIntoView()` が効いているか要確認
- 画面下固定（アンカー広告）にはしない。地図の表示面積を常時削るため
- レスポンシブ幅の広告ユニットを使い、パネル幅 372px に収まることを確認する

---

## 2. インフィード広告（PRカード）

**置き場所**: `site/app.js` の `inspect()` が組み立てる `#result` の innerHTML の末尾、
`gisLink(ward, lon, lat)` の**後**。

現在の並び:
```
区名 → 検索一致 → 緯度経度 → ピンの説明
  → cardYouto（用途地域）
  → cardKodo（高度地区）
  → cardBouka（防火）
  → gisLink（各区公式GISで開く）
  → ★ここにPRカード1枚
```

**なぜここが本命か**:
このサイトの利用者は「特定の土地を思い浮かべて建ぺい率・容積率・高さ制限を調べている人」。
つまり土地の購入・建て替え・相続した土地の活用を検討している層が濃く集まる。
一般的なブログとは読者の質が違う。

| | 想定収益 |
|---|---|
| AdSenseバナー | 0.1〜0.5円/PV。月1万PVで 1,000〜5,000円 |
| 不動産一括査定アフィリエイト | **1件 約25,000円**（成約1件 ≒ AdSense 5〜25万PV分） |
| 注文住宅の資料請求 | 1件 数千〜1万円台 |

**候補**: 不動産一括査定（HOME4U・イエウール・すまいValue 等）、
注文住宅の資料請求（LIFULL HOME'S・タウンライフ家づくり 等）。ASP経由で取得する。

**守ること**:
- **「PR」表記を明示する**。2023年10月施行のステマ規制（景品表示法）で、
  広告であることを隠すのは違法。表示は目立つ位置に、はっきりと
- 1画面に1枚まで。カードを積み重ねない
- 用途地域の判定結果そのものには一切干渉しない。
  情報カード（`cardYouto` 等）とPRカードは**視覚的にはっきり区別**する
  （背景色・枠線を変える、`.card` とは別クラスにする）
- 「この土地の相場を調べる」など、その瞬間の関心の延長にある文言にする。
  煽らない

---

## 独自ドメインへの移行（広告より先）

現在: `https://youto-map.summary301.workers.dev/`

**Cloudflare公式ドキュメントが本番利用を明確に非推奨としている**:

> It's recommended to run production Workers on a Workers route or custom domain,
> rather than on your workers.dev subdomain, as your workers.dev subdomain is treated
> as a **Free website** and is intended for **personal or hobby projects that aren't
> business-critical**.
>
> — [Cloudflare Docs / workers.dev](https://developers.cloudflare.com/workers/configuration/routing/workers-dev/)

理由:
- [ ] **AdSenseが通らない可能性が高い** — 2016年以降、新規申込は原則として独自ドメインが必要。
      大手無料ブログ以外のサブドメインは審査対象外になりやすい
- [ ] **信頼性** — 不動産の判断に使うサイトが `workers.dev` だと、開発中のテスト環境に見える
- [ ] **セキュリティ製品による一括ブロック** — `*.workers.dev` は
      プロキシや不審サイトにも使われるため、企業のフィルタでまとめて遮断されることがある
- [ ] **後から変えると損をする** — ドメイン変更には301リダイレクトと再インデックスが必要。
      被リンクも検索順位もまだ無い**公開前の今が、移行コストがゼロの唯一のタイミング**

**手順**:
1. ドメインを取得（Cloudflare Registrar は原価販売。`.com` で年 1,500円程度、`.jp` は 3,000〜4,000円程度）
2. Cloudflare でDNSを管理下に置く（Registrar で買えば自動）
3. Workers の設定から Custom Domain を追加（1クリック）
4. `workers.dev` 側からは301リダイレクトを張るか、公開前なら不要

**候補名の考え方**: 内容が伝わる短い英字。例 `youto-map.com` / `youtochiiki.jp` など。
公開後に変えられないので、ここだけは決め打ちする前に一晩置く。
